#pragma once
#include <string>
#include <vector>
using std::string;
using std::vector;

struct studentas {
    string vard, pavard;
    vector <float> nd;
    float egzam{};
    float gal{};
    float med{};
};